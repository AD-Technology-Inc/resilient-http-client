FROM python:3.12-alpine

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn

COPY mock_service.py .

EXPOSE 8080

CMD ["uvicorn", "mock_service:app", "--host", "0.0.0.0", "--port", "8080", "--log-level", "info"]
